color:
  palette:
    primary: "#3730a3"
    secondary: "#1a8a8a"
    accent: "#c69a2d"
    success: "#2d9050"
    warning: "#c69a2d"
    danger: "#b83a3a"
    info: "#3a6ab8"
    light: "#f3f2f7"
    dark: "#1a1625"
  foreground: "#2e2746"
  background: "#f3f2f7"

# Typography is handled via SCSS themes and _quarto.yml include-in-header
# because _brand.yml font loading conflicts with embed-resources: true
# (Quarto tries to fetch the preconnect URLs as resources, causing 404s).
# When Quarto fixes this interaction, move font config here.
