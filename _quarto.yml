project:
  type: default
  output-dir: _output

format:
  revealjs:
    theme: themes/impeccable.scss
    # Slide behavior
    slide-number: c/t
    hash: true
    hash-type: number
    history: true
    controls: true
    controls-layout: bottom-right
    progress: true
    center: false
    # Sizing
    width: 1920
    height: 1080
    margin: 0.05
    min-scale: 0.2
    max-scale: 2.0
    # Transitions
    transition: fade
    transition-speed: default
    background-transition: fade
    # Navigation
    navigation-mode: linear
    touch: true
    keyboard: true
    # Appearance
    auto-animate: true
    auto-animate-easing: ease
    auto-animate-duration: 0.6
    code-line-numbers: false
    highlight-style: github-dark
    fig-align: center
    embed-resources: true
    # PDF export
    pdf-separate-fragments: false

# Include custom fonts via Google Fonts
# Note: preconnect links removed — they cause 404 warnings with embed-resources: true
# because Quarto/Pandoc tries to fetch the bare preconnect URLs as embeddable resources.
include-in-header:
  text: |
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,wght@0,200..800;1,200..800&family=Source+Sans+3:ital,wght@0,200..900;1,200..900&family=JetBrains+Mono:ital,wght@0,100..800;1,100..800&family=Outfit:wght@100..900&display=swap" rel="stylesheet">
